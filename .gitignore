# Dependencies
node_modules/

# Build output
dist/
build/

# Environment files (contain secrets - never commit these)
.env
.env.local

# Uploaded files (local dev only - production uses cloud storage)
backend/uploads/*
!backend/uploads/avatars/.gitkeep
!backend/uploads/media/.gitkeep

# OS/editor files
.DS_Store
*.log
